<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lab3_email.tutorial.cs5511.com.lab3email">

    <uses-permission android:name="android.permission.CAMERA"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AccntActivity"
            android:theme="@style/accntstyle"
            android:label="@string/accntname"
            />
        <activity android:name=".RegistrationActivity"
            android:label="@string/RegName"
            android:theme="@style/regstyle"
            />
        <activity android:name=".LoginActivity"
            android:label="@string/LoginName"
            android:theme="@style/loginstyle">
        </activity>
        <meta-data android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="ocr"/>

    </application>

</manifest>